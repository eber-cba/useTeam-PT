# Base de datos
MONGO_URI=mongodb://localhost:27017/kanban-app

# JWT
JWT_SECRET=tu-jwt-secret-super-seguro

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-password-de-aplicacion
SMTP_FROM=noreply@kanban-app.com

# n8n webhook used by the export endpoint. Example: http://localhost:5678/webhook/xxx
N8N_WEBHOOK_URL=

# Comma-separated list of recipients that n8n should send the exported backlog to.
# Example: alice@example.com,bob@example.com
N8N_EMAIL_TO=

# Optional: subject/body for the email sent by the n8n workflow
N8N_EMAIL_SUBJECT=Backlog export
N8N_EMAIL_BODY=Se exportó el backlog. Revise el adjunto o el flujo en n8n para continuar.

# Frontend URL
FRONTEND_URL=http://localhost:5173
